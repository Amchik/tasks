
#ifndef _ZF_H_MESSAGES
#define _ZF_H_MESSAGES

// Prints [i] msg
void echo(const char* format, ...);
// Prints [w] msg
void warn(const char* format, ...);
// Prints [e] msg
void error(const char* format, ...);

#endif
